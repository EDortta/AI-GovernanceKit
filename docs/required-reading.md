# Required Reading

Documents an agent **must read before analysing or implementing any issue** in this
project, beyond the universal `AGENTS.md`, `docs/software-overview.md`, and
`docs/limits.md`.

List concrete project-specific documents below as bullets. Use `- (none)` if there
are genuinely none. `governancekit doctor` fails while this list is empty.

## Read before any issue

- `docs/software-overview.md` — product context and current scope
- `docs/limits.md` — hard operational boundaries
- `docs/codemap.md` — persistent code index (run `governancekit map` to refresh)

## Read for specific areas

<!-- Add area-scoped reading here, e.g.:
- `docs/project/architecture.md` — when touching the orchestration layer
-->
